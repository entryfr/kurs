# Отчёт по покрытию ТЗ: текущее состояние проекта

Дата: 2026-02-19  
Ветка: `cursor/-bc-9fa0f8b0-05e1-41b9-b8c5-6435a6630dd5-d2aa`

## 1. Что реализовано

## Архитектура и модули
- [x] Модульная структура `preprocessing / detection / classification / reporting / agent`.
- [x] Единый сервисный слой пайплайна `src/pipeline/service.py`.
- [x] CLI + Web используют общий `run_pipeline`.
- [x] Добавлен storage-слой (`src/storage`) для persistence запусков в БД.
- [x] Добавлен новый контур `src/ai_agent_course` (V2) для сценария "картинка + запрос".

## Входные данные (Этап B)
- [x] Магнитная сетка из `.npy`.
- [x] Магнитная сетка из `GeoTIFF (.tif/.tiff)`.
- [x] Аномалии + коммуникации из `.gpkg`.
- [x] Аномалии + коммуникации из `MIIS XML`.
- [x] Опциональные признаки из `SEG-Y` (`segy_reader.extract_segy_features`).

## Детекция/классификация/коррозия
- [x] Детекция линейных аномалий (OpenCV pipeline).
- [x] Порог обработки линейных аномалий `>10 м` (короткие линии отфильтровываются).
- [x] Классификация риска (`RiskClassifier`, fallback логика).
- [x] Приоритетная ТЗ-логика риска 89/62/12 c учётом зоны строительства и рассогласования глубины.
- [x] Выделение «слепых зон» и учёт в объяснениях риска.
- [x] Определение типа коммуникаций (`UtilityTypeIdentifier`, детерминированный fallback).
- [x] Оценка коррозии (`corrosion_estimator.py`) с предупреждением при `T_ост < 5 лет`.

## Нормативная валидация (Этап C)
- [x] Расширенная валидация с кодами правил `R01..R12` (`validate_anomalies_detailed`).
- [x] Агрегация нарушений по правилам (`summarize_issues_by_rule`).
- [x] Генерация отчёта валидации (`generate_validation_report`).
- [x] Профили нормативов: `demo / normative / strict`.

## Объяснимость результатов
- [x] `risk_details` по аномалиям:
  - confidence,
  - причины,
  - привязанные нарушения норм (`norm_violations`).

## Отчётность и экспорт
- [x] Экспорт `DXF`.
- [x] Экспорт `MIIS XML`.
- [x] Экспорт `MINS XML` (формат обмена для интеграции в контур города/Минстроя).
- [x] Экспорт `DOCX` с табличной частью.
- [x] Экспорт `PDF` акта (при наличии `reportlab`).

## Веб-интерфейс
- [x] HTML форма запуска.
- [x] Синхронный запуск из web.
- [x] Асинхронный запуск c `job_id`.
- [x] API статуса задачи: `GET /api/run/{job_id}`.
- [x] История запусков: `GET /api/runs` + виджет в UI.
- [x] Загрузка входных файлов через браузер (`UploadFile`).
- [x] Чат с агентом (`/chat`, `/api/chat`) и история чат-сессий.
- [x] API доступа к сохранённым DB-запускам (`/api/db/runs`, `/api/db/runs/{run_uid}`).
- [x] Отдельный V2 web/API (`src.ai_agent_course.web`) для прямого анализа изображения.

## API и эксплуатация
- [x] `healthz`.
- [x] Swagger (`/docs`).
- [x] Базовая защита путей (ограничение в пределах каталога проекта).
- [x] Устойчивый fallback chat-агента при таймаутах/ошибках провайдера.

## Тесты
- [x] Базовые unit/integration тесты ключевых модулей.
- [x] Тесты web API/UI (`tests/test_web_app.py`).
- [x] Тесты input ingestion (`tests/test_input_loader.py`).
- [x] Тесты 12-rule валидации (`tests/test_validator_rules.py`).

---

## 2. Что реализовано частично

1. **ML-модели как production-контур**
   - Есть интерфейсы и загрузка моделей.
   - Но fallback-режим всё ещё эвристический, нет полного MLOps контура.

2. **SEG-Y анализ**
   - Есть извлечение признаков.
   - Алгоритм гипербол пока упрощён (инженерный baseline, не финальная научная модель).

3. **Нормативы**
   - Добавлен набор `R01..R12`.
   - Требуется формальная трассировка `rule ↔ пункт СП` в виде отдельного нормативного реестра.

4. **История запусков**
   - Реализована через file-based store (`.cache/web/run_history.json`).
   - Для production лучше вынести в БД.

---

## 3. Что пока не реализовано (gap)

1. **Полноценный слой PostGIS/pgPointCloud**
   - Реализован базовый слой таблиц и API чтения.
   - Нет миграций Alembic, нет полноценной модели хранения облаков точек `pgPointCloud` (сейчас только `pointcloud_ref`).

2. **CI/CD и quality gates**
   - Нет полноценного pipeline с линтингом/типизацией/автотестами в GitHub Actions.

3. **Безопасность web-слоя уровня production**
   - Нет auth, rate limiting, audit trail пользователей.

4. **Оркестрация длинных задач уровня production**
   - Асинхронность реализована in-process (ThreadPool).
   - Для тяжёлой нагрузки нужен внешний брокер/очередь (Celery/RQ + Redis).

5. **Формальный датасет и метрики модели из ТЗ**
   - Нет включённого production-датасета (например, 1200 кейсов).
   - Нет автоматизированного отчёта метрик (precision/recall/F1/AUC) в CI.

---

## 4. Рекомендуемые следующие шаги

1. Вынести job-очередь из процесса приложения (`Redis + Celery`).
2. Добавить persistence запусков и артефактов в PostgreSQL/PostGIS.
3. Ввести строгие схемы `pydantic` для всех промежуточных объектов.
4. Добавить CI: `ruff + mypy + pytest`.
5. Реализовать слой авторизации в web/API.
6. Подготовить официальный "матрица соответствия СП" (rulebook) с ссылками на нормативные пункты.
